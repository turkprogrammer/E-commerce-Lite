# Конфигурация Validator
framework:
    validation:
        enabled: true
        email_validation_mode: html5
        mapping:
            paths:
                - '%kernel.project_dir%/src/Domain/Entity'

        # Автоматическая валидация типов
        not_compromised_password: false
